-spring:
  datasource:
    url: jdbc:mysql://localhost:3306/consuldb
    username: user
    password: password

server:
  port: 8081  

cloud:
  consul:
    discovery:
      service-name: consulclient
